route:
  receiver: default
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: default
    webhook_configs: []

# Telegram integration can be added via webhook relay (optional)
